name: Test Build

on: [push]
  
jobs:
  build-changelog:
  
    runs-on: ubuntu-latest  
  
    steps:
    - name: create text file
      run: | 
        .github/workflows/touch test.txt
        echo 'hello world'
