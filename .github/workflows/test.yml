name: Test Build

on: [push]
  
jobs:
  build-changelog:
  
    runs-on: ubuntu-latest  
  
    steps:
    - name: create text file
      run: | 
        touch ./github/workflows/test.txt
        echo 'hello world' >> .github/workflows/test.txt
